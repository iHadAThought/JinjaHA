# jinja-3.1 compatibility goldens

Upstream-shaped Jinja dialect cases for **JinjaCore** (no HA helpers).

Add `.jinja` + `.expected.txt` pairs here when tracking a new Pallets behavior on the 3.1 line.
For a new major/minor target, create `Compatibility/jinja-<version>/` instead.

See [`../TRACKED_DIFFERENCES.md`](../TRACKED_DIFFERENCES.md) and [`../../Docs/COMPAT.md`](../../Docs/COMPAT.md).
